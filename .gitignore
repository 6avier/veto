# --- Secrets ---
.env
.env.*
!.env.example
*.pem
*.key

# --- Python / Django ---
__pycache__/
*.py[cod]
*.so
venv/
.venv/
env/
db.sqlite3
db.sqlite3-journal
/backend/staticfiles/
/backend/media/
*.log

# --- Node / Vite ---
node_modules/
dist/
dist-ssr/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Editors ---
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# --- Local working notes, agent tooling and submission paperwork ---
#
# All of this still exists on the maintainer's machine; none of it belongs in a
# public repository. The engineering context that the rest of the docs cite now
# lives in docs/ENGINEERING.md.
#
# PROPOSAL+REGULATIONS/ holds the competition submission and the organiser's
# guidebook. The guidebook carries committee members' personal contact details,
# which is why it is kept out of the repository and out of its history.
.agents/
.claude/
.superpowers/
AGENTS.md
CLAUDE.md
docs/IMPECCABLE.md
docs/superpowers/
PROPOSAL+REGULATIONS/

# --- macOS ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Impeccable critique snapshots. Local design notes, not shipped.
.impeccable/

# Collected by manage.py collectstatic at build time. Never committed.
backend/staticfiles/
